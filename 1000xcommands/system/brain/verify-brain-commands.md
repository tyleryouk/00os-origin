# Verify Brain Domain Commands

# Objective: Automate the verification and initial correction pass for brain domain commands using terminal commands.

# Tool Calls

# 1. Read the governing process for context
read_file(
    target_file="1000xbrain/brain/processes/command-verification-and-correction.md",
    should_read_entire_file=True,
    explanation="Read the process definition for command verification and correction."
)

# 2. Execute the verification script
run_terminal_cmd(
    command="cd 1000xscripts/brain; ./generate_brain_command_index.ps1",
    explanation="Navigate to the script directory (relative to workspace root) and execute the brain command verification script.",
    is_background=False
)

# 3. Read the results generated by the script
read_file(
    target_file="1000xplans/brain/commands-index.md",
    should_read_entire_file=True,
    explanation="Read the updated command index file containing verification results."
)

# 4. Initiate Analysis and Correction Phase
# (1000xdev internal action based on the process read in step 1 and results read in step 3)
# - Parse the index file for flagged commands ([?] or [!]).
# - For each flagged command:
#   - Read the command file (1000xcommands/brain/...)
#   - Analyze against notes.
#   - Attempt automated fixes (edit_file) for simple issues (&& -> ;, add | cat).
#   - Log complex issues (append to planning/log file).
# - Summarize actions taken.
# (Actual tool calls for read/edit/logging would follow based on the analysis) 
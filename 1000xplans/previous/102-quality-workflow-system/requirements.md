## quality-workflow | system

Enhancing the plan-mode instructions in the core-brain-files, workflow-files, and project-rule-parameters.

Go through the current plan-mode process in the core-brain-files, and identify disrepencies in the workflow-files and the project-rule-parameters.

Find ways to enhance the core-brain-files on plan-mode, along with the plan-mode instructions in the workflow-files and the project-rule-parameters.

Additionally, it must be clear that every plan-mode will start with a planning folder loaded into the Context Pills. The planning folder will only have 

I just want you to go through the instructions of the plan-mode process within 1000xbrain because I feel like there are many times when you ask me for clarity or jump to implementation during plan-mode. A lot more hallucinations are starting to arise now and I want to know the root cause. The process should be documented as strict and autonomous in the core-brain-files, then the details should be specified in the workflow-files and the project-rule-parameters.

## Key System Structure Clarification

It is critical to understand the correct system structure of 1000xbrain:

1. **1000xbrain is the SYSTEM** - The overall cognitive architecture system.

2. The system contains FOUR distinct SUBSYSTEMS:
   - **Core Subsystem**: Contains core-brain-files with Always rule type
   - **Knowledge Subsystem**: Contains knowledge-base-files with Agent Requested rule type (requires # USE WHEN header)
   - **Parameters Subsystem**: Contains project-rule-parameters with Manual rule type (requires specific header format)
   - **Workflows Subsystem**: Contains workflow-files with Auto Attached rule type

3. Each subsystem contains documentation for all five workflow types (rules, quality, front-end, back-end, scripts).

4. This distinction is critical for proper understanding of the system architecture and avoiding hallucinations during implementation.
# Potential Future Enhancements: Back-End

*   **Log Migration:** Migrate relevant application logs currently in `/logs-main` into a structured logging solution within the `/back-end` directory (e.g., `/back-end/logs` or integrated with application logging framework). Define log format and rotation strategy.
*   **(Add other future backend enhancements here)** 
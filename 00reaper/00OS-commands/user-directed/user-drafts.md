Add folder .cursor\rules\00os which will hold the subfolders \config, \core and \processes. Have AI read latest news on /Generate Cursor Rules function. I want to be able to utilize this new tool by Cursor, which should write context-based rules to .cursor\rules which I can attach to next thread.

